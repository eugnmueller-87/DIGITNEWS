// Empty stub for the `server-only` package under Vitest's node environment.
// `server-only` is a Next.js build-time import guard with no runtime surface;
// tests that pull in modules importing it just need it to resolve to nothing.
export {};
